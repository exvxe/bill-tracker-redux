@import '_colors.styl'

.barCharts
    display flex
    align-items center
    padding 20px 0
    .barChart
        border-right 4px c_accent solid
        padding 20px 0 20px 40px
        &:last-of-type
            border none